/*
Extra configuration for nf-test runs. The tests themselves either run in stub
mode, so that no tool has to be installed, or use the python container.
*/
params {
    max_cores = 2
    cores = 1
    memory = '2 GB'
}
